<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.john.dao.TAssetClassMapper">

    <select id="getAllClass" resultType="cn.john.dto.ClassVo">
        select id, class_name className from t_asset_class where is_del = 0 order by sort
    </select>
</mapper>
